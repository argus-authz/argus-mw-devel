# Required metadata
sonar.projectKey=argus-mw-devel
sonar.projectName=Argus authz Puppet Module
sonar.projectVersion=0.1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=.
